import { ProducHostDirective } from './produc-host.directive';

describe('ProducHostDirective', () => {
  it('should create an instance', () => {
    const directive = new ProducHostDirective();
    expect(directive).toBeTruthy();
  });
});
